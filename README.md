# Informatics Monitor Link Builder

Удобный инструмент для создания ссылок на монитор результатов **нескольких контестов курса** с возможностью указания временных границ посылок. Всё реализовано через интерактивный CLI на базе `inquirer`.

## ⚡ Возможности

* Выбор одного или нескольких контестов из курса.
* Выбор одной или нескольких групп для мониторинга.
* Задание временных границ (`time_after`, `time_before`).
* Генерация готовой ссылки для мониторинга с возможностью копирования в буфер.
* Интерактивный CLI с пошаговым выбором и удобным интерфейсом.

## 🛠 Установка

### 1. Клонирование и ручная установка зависимостей

```bash
git clone <URL_репозитория>
cd <имя_папки_проекта>
pip install -r requirements.txt
```

### 2. Использование виртуального окружения

* Linux/macOS:

```bash
bash setup/setup.sh
```

* Windows:

```cmd
setup\setup.bat
```

> Скрипт создаёт venv в корне проекта и устанавливает зависимости из `requirements.txt`.

## 🔑 Настройка переменных окружения

Пример находится в файле `.env.example`:

```bash
INFORMATICS_USERNAME=your_username
INFORMATICS_PASSWORD=your_password
```

* Скопируйте файл `.env.example` в `.env` и укажите свои данные.
* Так можно не вводить логин и пароль вручную при каждом запуске.

## 🚀 Запуск

```bash
python main.py
```

* Программа пошагово предложит:

  1. Выбор курса
  2. Выбор контестов
  3. Выбор групп
  4. Ввод временных границ
  5. Выдаст готовые ссылки

* Ссылка отображается в терминале и, при установленном `pyperclip`, автоматически копируется в буфер обмена.

## 📝 Структура проекта

```
informatics_monitor/
├── main.py                  # Точка входа
├── requirements.txt         # Список зависимостей
├── .env.example             # Пример переменных окружения
├── .gitignore
├── tests/                   # Различные unit-test'ы
├── modules/
|   ├── informatics_api.py   # Работа с сайтом Informatics
|   ├── menu.py              # CLI с выбором курсов, контестов и групп     
|   └── utils.py             # Утилиты: сбор ссылок, чтение env и др.
└── setup/                   # Скрипты для создания venv и установки зависимостей
    ├── setup.sh
    └── setup.bat
```

## 🧩 Зависимости

* Python 3.10+
* requests
* bs4
* selenium (для получения групп)
* inquirer (CLI)
* pyperclip (для автокопирования ссылки)

## 🔄 Контрибуторам

1. Создать новую ветку:

```bash
git checkout -b feature/your-feature
```

2. Сделать изменения и коммит
3. Открыть Pull Request

## ⚠️ Важно

* **Не забудьте добавить `.env` с логином и паролем в .gitignore!**
* **Для того, чтобы работал монитор, необходимо зайти в свой аккаунт на informatics, иначе будет выдавать HTTP 403!**